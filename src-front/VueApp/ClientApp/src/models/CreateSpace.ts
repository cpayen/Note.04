export class CreateSpace {
  public name!: string;
  public slug!: string;
  public description?: string;
  public color!: string;
  public ownerId!: string;

  /**
   *
   */
  // constructor(name: string, slug: string, description: string, color: string, ownerId: string) {
  //   this.name = name;
  //   this.slug = slug;
  //   this.description = description;
  //   this.color = color;
  //   this.ownerId = ownerId;
  // }
}
